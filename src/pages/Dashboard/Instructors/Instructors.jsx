import { useQuery } from '@tanstack/react-query';
import React from 'react';





const Instructors = () => {
    return (
        <div>
          
        </div>
    );
};

export default Instructors;